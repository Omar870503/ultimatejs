let numero = 42;// Declaramos una variable llamada "numero" y le asignamos el valor 42
let nombre = "Omar";// Declaramos una variable llamada "nombre" y le asignamos el valor "Omar"
let verdadero = true;// Declaramos una variable llamada "verdadero" y le asignamos el valor true
let undef;// Declaramos una variable llamada "undef" sin asignarle un valor, por lo que su valor será "undefined"
let nula = null;// Declaramos una variable llamada "nula" y le asignamos el valor null

console.log(typeof numero);
console.log(typeof nombre);
console.log(typeof verdadero);
console.log(typeof undef);
console.log(typeof nula);