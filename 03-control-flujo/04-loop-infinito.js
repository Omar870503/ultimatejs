let i = 0;

while (i < 10) {
    console.log("Numero: " +i);
    i++;// si no incrementamos i, el ciclo se ejecutara infinitamente, 
    // cuidado con los ciclos infinitos, pueden bloquear tu navegador o 
    // tu computadora, si esto sucede, puedes cerrar la pestaña o el programa 
    // para detenerlo.
}